import fs from 'fs';
import path from 'path';
import { OHLCVT, TickDataArray } from '../../types';

// Data to append (as an array )

export default async function writeUpdateToSql(ohlcvtArray: TickDataArray[]) {}
